[Unit]
Description=Monitor Docker events and update /etc/hosts
After=docker.service
Requires=docker.service
Wants=docker.service

[Service]
Type=simple
ExecStart=/usr/local/bin/docker_etc_hosts --watch
Restart=always
RestartSec=5
User=root

[Install]
WantedBy=multi-user.target
